# 经验沉淀

- [由于复制重命名未修改额外属性导致Fits节点定位错位] -> [在RigSystem的rig_rml获取surface、up、dn时，解包前没有做节点验证导致TypeError] -> [在rig_rml循环中执行前，获取相关节点/属性字典后显式校验并输出cmds.warning] -> [避坑规则：任何受用户自定义属性（如degree, joint, cluster）影响生成的解包操作前，必须进行字典空值或键缺失的可用性校验]
- [替换原有 BlendWeighted 阵列重写为 WtAddMatrix 时生成的绑定表现变形不连贯或破碎] -> [WtAddMatrix 需要的是相对基准静置属性（M_diff），错误把动态的世界矩阵 M_world 连入了计算导致增量炸裂] -> [保持 Weight.get 依然通过获取一次性的 cluster["worldMatrix[0]"] 用 add_pose() 计算出偏差常量输入] -> [避坑规则：在 MFace2 的原生 Weight 生成中，Cluster 只当作提取基础偏移的标识点容器，绝不在装配期建立动态约束]
- [WtAddMatrix 合并后由于手动编写基准向量计算带来骨架大幅度脱出和无响应] -> [用写死的 [0,1,0] 乘聚合矩阵计算极轴，强行默认了骨架绑定前是正交世界坐标的，一旦骨架带原始角度则会连带控制器一起翻转失效] -> [全面剥离人工参与，利用 decomposeMatrix 节点直接抽取 M_total 的 outputRotate 反哺源生通道] -> [避坑规则：处理混合矩阵 (M_rest * M_sum) 提取旋转时，永远优先使用最稳的 decomposeMatrix 而非粗暴抓取分轴！]
- [导出pose菜单无响应] -> [ui/facs.py中addAction未提供回调函数参数] -> [补全回调参数为save_json(tools.save_face_pose_data)] -> [避坑规则：UI编写时必须确保所有菜单动作均绑定到有效的闭包或回调，杜绝空行为挂载]
- [自定义属性驱动控制控制器的缩放二次放大/缩小跑偏] -> [core.py中联合矩阵解析缩放时，将三维向量各分量平方和再次平方(sum**2)而非开方] -> [修正缩放计算提取公式，将指数由 ** 2 改为 ** 0.5 (即开根号)] -> [避坑规则：从矩阵直接解算点位或缩放等数值(Magnitude)时，必须做数学正确性验证，切忌全凭经验手写公式]
- [右键修改驱动目标时阈值不同步] -> [原有edit_target仅记录或挂载被驱动对象的形变，未检测并更新主驱动器阈值] -> [提炼check_and_update_threshold方法并在edit_target首行调用，检测差值弹窗更新SDK关键帧] -> [避坑规则：复合更新关联操作（修改Target）时，必须同步确认输入端（Driver）和输出端（Driven）的数据有效性，确保上下游一致]
- [核心逻辑重复与修改状态未全面拦截] -> [修改与复制修改在处理非模型目标时底层路由一致，但修改前置的阈值验证未同步覆盖引发布局漏洞] -> [提炼安全拦截器统一部署，并结合is_on_duplicate_edit判定二次确认提交状态避免提前弹窗] -> [避坑规则：强健的拦截和提示逻辑（如阈值检查、成功弹窗）必须向内收束注入在核心公用执行层，不能散落在外围调用堆栈中]
- [系统新加入记录不跟随焦点导致操作断裂] -> [UI层点击添加后列表仅reload死等操作，原因在于底层@undo等公共保护类装饰器吃掉了内部原本有的返回值] -> [在装饰器剥离提取核心运算结果(res = fun())并将其透传return，同时在UI端捕获该回传后强制介入scrollToItem与Slider满格逻辑] -> [避坑规则：任何用于包裹底包拦截上下文环境（如 undo、profiler 等）的 Wrapper 装饰器，必须绝对透明地流转 *args, **kwargs 与 return res，禁止“黑箱截断”。]
- [修改或复制修改形变目标时阈值平移引发被驱动体数值跳闪污染] -> [当场修改SDK的阈值曲线瞬间触发了Maya的节点图(DG)全剧刷新，导致原本肉眼可见的2.5形变在骨骼提取前被强制算成了5.0的满权重] -> [采用“延期执行(Deferred Execution)”模型，将更新阈值的代码打包装进一个闭包函数中，待骨骼或模型的数据抽取完全结束后再触发回调] -> [避坑规则：在涉及当前视窗矩阵或点位的捕获操作前，**绝对禁止**任何直接修改上游动画曲线、约束权重等会触发DG级联重刷的危险操作，必须将修改意图闭包暂存到整个提取烘焙管线末端统一消费！]
- [更新驱动阈值后控制器位置物理固化引发二次计算错觉] -> [闭包回调修改SDK曲线后，控制器的物理值并未主动回归到符合新阈值标准的刻度(如0.5)，依然僵滞在受提取器强行重置出的1.0位置，导致用户下一次拉动滑栏时基于1.0错误映射而发生了形变叠加放大的二次计算] -> [在回调中执行完曲线平移编辑(keyframe floatChange)后，必须追加cmds.setAttr手柄硬重置，强制将控制器物理落位在新的阈值定点] -> [避坑规则：所有对动画联动关键节点的底层输入阀值（SDK/Driver）发生非线性变更后，**必须配对同步复位该控制单元的视窗属性(Physical UI Value)**，彻底阻断旧缓存带来的映射雪崩！]
- [多边形模型目标“复制修改”逻辑时，用户滑栏设置的新驱动阈值被完全无视] -> [执行多边形开始克隆雕刻之前，由于错误地将“阈值变更确认”拦截放在了全流程收尾(is_finishing=True)的提交结算环里，导致起始阶段的克隆提取使用的是被逼迫向1.0对准的旧数据] -> [进行生命周期前置隔离，在复制修改发源期(is_finishing=False)立马阻截并在提取克隆前消费完毕最新的修正曲线阈值，然后才执行拓扑克隆供雕刻] -> [避坑规则：凡涉及“开启态”与“封口态”两步走的多模型克隆操作，任何有关网格提取时驱动阀值/源形态特征确权的指令，**必须且只能放在开启态第一阶段彻底消费掉**。！]
- [更新阈值后交互“逻辑不顺”，后续轻触UI滑栏导致被驱动物体狂跳] -> [虽然底层彻底跑通了从0.5更新阈值并安全记录的任务，但是前台UI的进度滑栏还死死地停靠在“旧的”30刻度，等下一次拨动滑栏时，UI框架基于30刻度算出错误映射传给底层，引发跳闪] -> [废除旧版静默代理包装“修改(edit_target)”与“复制修改(auto_duplicate)”，转而显式包裹一套Action动作连招，在编辑结束的最后一步强行调用UI自带的_auto_select将焦点捕捉，并将滑栏刻度强硬锁定至60满偏状态同步] -> [避坑规则：所有UI驱动底层的修改（特别是“滑块调节重度绑定的阈值缩放”），在其修改定型后，**视窗UI焦点必须被强力拉回 100% 相对位置（例如最大度数60）**以抹平认知代沟，绝对不允许“底层宇宙全变了，表层UI刻度还定在半当间”的脱节！]
- [多边形克隆修改时彻底违背“所见即所得”，导致用户面前的模型突变为旧版满编形态] -> [底层克隆方法`to_pose`强制为了隔离出单靶点，将用户的目标控制手柄强行推向了旧曲线的满偏值（1.0），导致用户期望在0.5设新阈值时抓拍到的2.5画面瞬间暴走变成了5.0，逼迫用户去雕刻一个错误放大的基础网格] -> [在生成环节部署一个闭包函数`clone_to_pose`拦截系统的克隆隔离指令，在系统切断其它靶点后，强行将焦点靶点的手柄刻度重新置回用户预期的缩放值（如0.5），强行以非全权重的缩放态克隆出网格，随后再执行曲线100%满偏度的阈值交棒，实现完美衔接！] -> [避坑规则：任何**带有基础网格剥离与替换特性的雕刻工作流（如Blendshape底层抓取）**，绝不容许无脑推拉1.0最大极值；必须记录并还原触发时刻的绝对数学形态位阶（物理映射快照），确保被抽出当做白板雕刻的网格100%贴合用户屏幕前的视窗快照！]
- [在控制器驱动场景下进行“修改/平移驱动阈值”，视窗发生了意料之外的属性跳跃] -> [开发者试图保护原始极限形变不被等比压缩阉割，因此在提取被驱动对象位置前先强制执行了阈值更新。这导致对象依据1.0的新满额权重瞬间跃迁至极限位置，违背了绑定师严格要求的“所见即所得”原则] -> [彻底恢复执行顺序：`edit_joint_target` 和 `duplicate_edit` 必须在 `apply_fns()` 更新阈值动作之前执行。系统必须依据旧阈值在当前刻度折算出的实际表现（例如0.5倍数）捕获视窗画面并固化为新上限，随后才由 `apply_fns()` 平移曲线。这一“先照相提模，后交接阈值”的流转逻辑，在数学上完美实现了目标的等比衰减与0跳动。] -> [避坑规则：执行系统级**驱动阈值平移（SDK调整）**时，绝不能擅自替用户“保留隐性资产极值”；只要用户的核心诉求是“不允许任何视窗跳跃”，唯一的正解就是**直接牺牲并等比裁剪 Target 目标的上限容量**去迎合当前的视窗快照！！一切空间抓取动作必须排在动画曲线变更前！]
- [在非满额权重处重新提取骨骼绑定修正姿势] -> [旧代码的预处理在提取前强行把原 Driver 控制器设值为满额(1.0)，导致 `add_pose()` 底部的除数 `weight` 强制变成了 `1.0` 而不是真实比例] -> [在触发提取的核心环节抓下真实权重快照，屏蔽 `Ctrl.reset_all()` 归零后推往 `1.0` 的跳转动作，将快照真实滑栏值塞回控制输入，让 `add_pose` 以真实的非1满额系数计算并放大出100%极限Target] -> [避坑规则：处理任何“基于当前滑动权重重新烘焙设定 1.0 上限结构极值（Target Point）”的业务流时，绝对不应当干涉或者更新用户的 UI 触发阈值，而是直接**保留真实驱动刻度，依靠除法代差(A/x)反向膨胀算出极限点**！]
- [更新阈值前置导致形变捕获失败] -> [修改驱动极值阈值会瞬间触发Maya DG解算(因SDK改变权重突跳1.0)，导致视口中的自定义雕刻点阵被强行还原并吸附回旧极限形状，后续执行点位提取时抓取到的全是旧数据(导致delta=0，Input值无更新)] -> [必须严格遵循“后置更新”原则，将静默更新阈值的 auto_update_threshold 操作完全推迟到所有形变捕获提取与节点写入运算执行结束的最末尾] -> [避坑规则：涉及底层驱动权重的阈值平移修正时，绝对禁止“先改阈值再取形变”，必须“先以现有残缺权重捕捉用户的视窗快照并运算落入BlendWeighted，最后再平移阈值使得整体新权重达到1.0满偏”，以避免视图被DG提前刷新污染！]
- [克隆修改多边形目标时，触发UI事件导致驱动强行归位] -> [克隆启动时未提前更新阈值极值，导致UI自动同步滑栏时依据旧极值拉平了用户的当前控制位移] -> [将更新阈值的动作(auto_update_threshold)前置到底层多边形克隆(clone_to_pose)之前发生] -> [避坑规则：任何涉及到平滑交接且依赖最新极值参数重配连带关系的操作，必须确保在引信(如UI滑栏事件)被点燃前使底层参数确权到位！]
- [右键放弃多边形克隆修改时，模型可见性连接未恢复且错误进入提交循环] -> [UI侧的取消事件结束后跳出了判断无意走进了后续代码(apply)流，且底层旧的撤销管线绑定了强制保存形状(edit_target)] -> [在UI内拦截(return)执行流直接跳脱apply逻辑，并在底层提供仅安全摘除视图控制节点和句柄的专属解绑函数(cancel_duplicate_edit)] -> [避坑规则：UI框架内的交互“取消(Cancel)”绝不可与“执行成功提交(Apply)”共用底层归档管栈，退场和交卷逻辑必须彻底物理隔离！]
- [添加/修改极值操作时的消息回显不统一行为] -> [原本系统在是否选择模型时，因为未选模型时的阻断导致漏报，或是调用了重复更新逻辑导致双重提示] -> [拦截非模型对象的独立更新判断，引入带有静默降噪(silent)的更新探测器收拢结果，最终统一吐出带有一致性格式的回执消息] -> [避坑规则：核心驱动逻辑在遇到不满足深层功能条件时(如未选中模型多边形)，应当干脆利落切入常规底层流中闭环处理，绝不允许散接多套相似语境的面条代码。]
- [系统UI列表与底层脱钩导致的“双向割裂感”] -> [旧版本的双击事件仅仅将信号转发到了底层动作(tools.set_pose)中强行推起了模型状态，但UI层面的权重调节滑块对此毫不知情依然停留原位待机] -> [将点击信号私有回调化，于底层施加完形变指令返回后，强制利用 `_auto_select` 工具让 UI 滑栏光标硬置于相应刻度顶端位置] -> [避坑规则：任何由视窗UI列表点击发起并改变三维物理引擎坐标参数的交互（特别是目标重定向），事后必须利用回流闭环或回调(Callback)将控制面本身的计量工具一并重配同步满偏！不许底层发车了前端还没动！！]
- [默认目标流转UI混乱无法识别结构及缺少空白驱动骨架展现] -> [旧代码使用最简单的线形动态加载 ListWidget 展现一切杂乱驱动，无法映射未创建的坐标轴位点，全靠用户人肉背记盲试盲拉操作] -> [从零开发 `TargetGrid` 网格矩阵继承于标准 `QTableWidget` 组件代替：主动向所选控制器发起请求以获得三维正负全参数集合，在网格铺开坐标系及UD节点，使用灰色展示坑位预留位置，当监测到挂载时使用高亮绿标识。使绑定面由抽象的“盲人摸象流串”升级成“填空控制板”] -> [避坑规则：面向复杂工程化环境的控制器绑定工具，其驱动池展现绝对不应当仅限于“建立好的残缺数组流水长列”，而是需要提前穷举并“填表格(Matrix Map)”一般向绑骨师展现物理全局观。必须对症挂载，以降低生产环境心智耗损！]
- [TargetGrid空驱动双击或操作抛出异常报错] -> [原本列表中的项一旦呈现在UI上即认为可操作，现TargetGrid引入了不存在的坑位预留行，用户点击或尝试修改这些虚拟项时底层因为找不到节点报错] -> [对 UI 的双击及提交操作增加 `tools.exist_target(t)` 安全阻断判定，并将所有的 `QTableWidgetItem` 加入 `item.setFlags(item.flags() & ~Qt.ItemIsEditable)` 禁用双击可编辑文字行为] -> [避坑规则：引入虚实结合的矩阵展示界面时，务必在触发器级防范“假行”向下游发射带毒空指针！]
- [模型与非模型选择条件下，“修改/复制修改”对 Joint 烘焙的行为分歧] -> [原本 `auto_duplicate_edit` 等动作内部带有条件分发，只有符合“选中骨骼”或“不仅选中多边形”的特定条件时才调用 `edit_joint_target` 参与底层参数更新计算] -> [取消一切有选择前提的调用阻断。使得“修改”和“复制修改”哪怕在没选东西、选了控制器、或是选择了模型进阶克隆操作时，底层永远雷打不动先以当前滑块比例执行一次 `edit_joint_target(target)` 基础同步，并将UI光标推至满偏态确保视觉对齐] -> [避坑规则：核心驱动权重的管理不应因为用户的附加意图（如正在选择多边形准备修型）而阻断原有的基础烘焙刷新动作。系统多模态操作必须构建在坚如磐石的基础参数自动托底之上！]
- [二次修型时撤销(Cancel)动作导致第一轮修型目标被彻底清空归零] -> [进入修型动作时，为保证当前雕刻动作不受已有的Blendshape形变叠加污染（因为被雕刻网格本身继承了当前视窗1.0满偏的Mesh快照），底层 `LEditTargetJob` 会在前置环节通过 `init_target` 洗空已有的原始形变坑位。而撤销逻辑之前仅仅暴力删除了临时雕刻节点，并未恢复那些被洗空的旧数据] -> [为 `LEditTargetJob` 植入类属全局字典存储结构 `_BACKUP`，在系统排空数据前，自动抓取并内存驻留原始点位云图。在用户触发“撤销修改”流时接管网格清理的尾部，将该字典内初版形态数据重新 `bs_api.set_bs_id_point_map` 写入目标，实现全状态无损回档。] -> [避坑规则：针对任何“为保证修改基线平坦而暴力前置排空原有底层缓冲”的修改流，必须在工具系统级自建完整的 History Backup 快照池！]
- [尝试备份缓存却遭遇 module 'bs_api' has no attribute 'get_bs_id_point_map' 闪退中断] -> [系统底层的 C++ `.pyd` 扩展库实际上并未向 Python 级别公开原生的字典或数组抽取接口，此前开发经验主义发作凭借猜测强行调用了该未暴露的存取方法导致运行时崩溃，同时也连带使得后续的界面标红以及“放弃修改”菜单监听事件全部因为崩溃阻断而惨遭失效！] -> [废除 Python 侧私有对象的数组存取备份法，全部退回利用 `bs_api` 稳健公开的黑盒环境缓存 API `cache_target_points` 写入 C++ 底层序列并利用 `load_cache_target_points` 无损逆读] -> [避坑规则：处理跨编译语言（特别是 C++ 封装打包的闭源模块）工具库通讯时，**永远不要先入为主去造句或揣测私有读写(getter/setter)变量的拼写存在！**遭遇缺失时，必须立刻止损利用 `dir()` 探明或调用系统官方提供的配套抽象 `cache/load` 封装去曲线救国。]
- [完全照做并且重启了 Maya，但旧版形变依然像患了失忆症一样彻底丢失] -> [此前虽然我在底层的 `bs_api.py` 中完美修复了 C++ 残留的数组重置问题，但忽视了用户极其危险的运行环境：**只要项目目录下依然存在被优先读取的 C++ `.pyd` 编译包，所有针对 Python 备用文件里的 Bug 修复都会被彻底无视！**底层依旧在跑有毒的老 C++ 库，导致历史不断被洗空洗白！] -> [对于第三方闭源编译库出现如此根深蒂固又不可触碰的结构性 Bug (例如无差别清空全局 Array)，绝不能对“兼容修复”抱有任何幻想。直接一刀切在 `bs.py` 调用源头实施“手术绕道”，全盘弃用它的缓存读写黑盒方法，使用绝对原生、暴力且安全的 Maya 命令 `cmds.getAttr` 和 `cmds.setAttr` 去精准提取节点最底层的 `inputPointsTarget` / `inputComponentsTarget` 并暂存在内存中。（随后也为了迎合并对齐旧版的原生 C++ 工作流，我顺水推舟直击源码将其编译重构解决了）] -> [避坑规则：**永远不要信任并试图通过更新 Fallback(回退) 脚本去“修补”以 .pyd / .dll 形式存在的 C++ 编译插件带来的逻辑死锁！** 一旦确信闭源底层不可靠，唯一、绝对 100% 有效的做法就是在主主控流程动刀。要么从外围直连读取拦截污染，要么潜入深水区打入原作者的独立编译包完成二次接种。]
- [永远不要凭借字面意思去瞎猜任何一个工程自定义函数的用意！] -> [哪怕它叫 `is_shape`，它也可能干的是 `has_mesh` 的活！在开刀修改别人的代码逻辑分支前，必须深入跳转查看底层验证！]
- [局部环境覆盖全局相对包导入导致莫名的 ModuleNotFoundError] -> [在头部具有 `from .. import tools` 时，函数内部顺手写的 `import tools` 会强制系统抛弃同级目录，去系统环境中寻找并不存在的根节点 tools] -> [任何情况下都应审视头部的相对依赖网络，如果在系统封装或 UI 子包中写代码，**彻底封杀所有函数内部的局部同名 import**！] -> [避坑规则：局部 import 极易产生遮蔽(Shadowing)，在包含相对引用树的定制工具架开发中，必须复用顶层名柄！]
- [为了防自激震荡而冻结主控件发信，却连带令旁边的依赖显示失去同步] -> [在 UI 联动取值时，为防止 `slider.setValue` 触发写回底层的闭环，我对它执行了 `blockSignals`，但这直接切断了原生绑定给 `QSpinBox` 的驱动链，导致只有滑条在动而数字死灰] -> [任何有关【UI元件互绑】的值更新，若是出于保护目的冻结了发信源，则必须将链条上的**所有关联显示端一并手动纳入显式赋值与静穆列表**] -> [避坑规则：**截断发射器等于斩断所有连体婴**。如果你 block 了一个触发器的主控件，就要担起重制其所有寄生状态组件表现的责任！]
